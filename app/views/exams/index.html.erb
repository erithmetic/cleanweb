<% content_for :title do %>Listing Exams<% end %>
<div class="blurb">
<table>
  <tr>
    <th>Winner</th>
    <th>Pencils down at</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @exams.each do |exam| %>
  <tr>
    <td><%= exam.winner_id %></td>
    <td><%= exam.pencils_down_at %></td>
    <td><%= link_to 'Show', exam %></td>
    <td><%= link_to 'Edit', edit_exam_path(exam) %></td>
    <td><%= link_to 'Destroy', exam, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Exam', new_exam_path %>
</div>
